# 测试与端到端验证记录

> 最后更新:2026-07-04

## 摘要

2026-07-04 完成三轮验证:测试 1(文本对话,成功但首次 ttft 176 秒)、测试 2(工具调用创建文件,12 秒,成功)、端到端验证(埋 bug → 委派 `claude-m3` 定位修复 → 主 session 独立复查,25 秒,5 轮工具调用,$0.34,通过)。三轮均未触发人工确认卡顿,`permission_denials` 全部为空。

## 一、测试1:文本对话

```bash
claude --settings ~/.claude/settings-mmx.json -p "用一句话介绍你自己" --output-format json
```

- 结果:`"我是 Claude Code,由 MiniMax-M3 模型驱动,是你的 AI 软件工程助手。"`
- ttft 176 秒(冷启动?),`modelUsage` 正确标注 `MiniMax-M3[1m]`
- 详见 [[MiniMax-M3协议兼容与网关]]

## 二、测试2:工具调用

```bash
cd /tmp/mmx-test && claude --settings ~/.claude/settings-mmx.json \
  -p "在当前目录创建一个 test.txt 文件,内容写 hello from M3" \
  --allowedTools "Write" --output-format json
```

- 结果:12 秒完成,`permission_denials` 为空,文件真实落地且内容正确(`hello from M3`)
- 验证了 [[执行委派规则]] 里"必须带 --allowedTools"这条规则的必要性和有效性

## 三、端到端验证:calc.py bug 修复

在 `/tmp/mmx-e2e-test/` 埋了一个真实 bug:`calc.py` 的 `subtract` 函数误写成 `return a + b`,导致 `test_calc.py` 里的 `test_subtract()` 断言失败(基线已用 `python3 test_calc.py` 独立确认,exit code 1)。

委派给 `claude-m3` 的 prompt(自包含上下文范例):

```
你在目录 /tmp/mmx-e2e-test/ 下工作,里面有 calc.py 和 test_calc.py 两个文件。
运行 python3 test_calc.py 目前会因为 AssertionError 失败。请你:
1) 读取 calc.py 和 test_calc.py 找出具体是哪个函数有 bug;
2) 修复 calc.py 里的 bug(不要改动 test_calc.py);
3) 重新运行 python3 test_calc.py 确认能看到 ALL TESTS PASSED 且退出码为 0 之后再结束任务。
```

`claude-m3` 用 5 轮工具调用、25 秒、$0.34 完成任务并自我报告成功。

## 四、独立复查结果(未采信自我报告)

主 session 没有直接相信 `claude-m3` 的自我报告,而是独立复查:

| 检查项 | 结果 |
|---|---|
| `calc.py` 是否真的改成 `a - b` | ✅ |
| `test_calc.py` 是否被误改 | ✅ 未改动,内容与委派前一致 |
| 独立重跑 `python3 test_calc.py` | ✅ `ALL TESTS PASSED`,exit code 0 |
| 是否有权限确认卡顿 | ✅ `permission_denials` 为空 |

这次完整走通了闭环:主 session 埋任务上下文 → `claude-m3` 独立读文件、改代码、跑测试、自我验证 → 主 session 独立复查确认。

## 关联主题

[[双模型编排方案概览]] [[MiniMax-M3协议兼容与网关]] [[claude-m3脚本与项目配置]] [[执行委派规则]] [[待澄清与后续事项]]

## 来源

- `raw/2026-07-04-conversation.md` 第 2、4、6 节
